# Pythia Athletics has no server-side secrets. Both the Anthropic API key and
# The Odds API key are entered by each user in the browser (stored in
# localStorage only, sent per-request) — there is nothing to put in .env.local
# for this app to run.
